Debug
	- Test and fix Shape2
	- Fix object copying subcalls
	- Make sure objects are properly added and deleted form the engine
	- test the new Object Inheritance chain

Style

Refactor
	- Move physical properties from base to physic obj
	- Use Shape2 for Objects instead of current setup
	- Use Precise ints : (u)intx_t, posint_t, etc
	- Use fixed_t instead of floats
	- Use angle_t instead of float
	- Use chain_t instead of List
	- Use bank_t< 2 > instead of Vect2
	- Rework the input system ( why not just use raylib inputs directly )

Performance


General
	- Use ScriptObject as an Entity Component System Interface
	- replace EXIT_SUCCESS with true & so on
	- Implement a timescale engine var

Nomenclature
	- set( val )         : v0 = v
	- move( delta )      : v0 += d
	- change( factor )   : v0 *= f
	- nudge( target, % ) : v0 += ( % * ( v - v0 )) : aka move a % towards a target